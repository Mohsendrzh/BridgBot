version: '3.8'

services:
  bot:
    build: .
    container_name: bridgegpt_bot
    restart: unless-stopped
    volumes:
      - ./bot_data:/app/data
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DATABASE_URL=sqlite+aiosqlite:///data/bot_database.db