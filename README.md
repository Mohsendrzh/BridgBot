# BridgeGPT Telegram Bot

یک ربات تلگرام مدرن، غیرهمگام (Asynchronous) و Stateless برای مدیریت تسک‌ها و استعلام قیمت ارز دیجیتال.

این پروژه به عنوان یک تسک فنی طراحی شده و تمرکز اصلی آن بر روی معماری تمیز (Clean Architecture)، مدیریت وضعیت در دیتابیس (Stateless) و کدنویسی مقیاس‌پذیر است.

## 🌟 ویژگی‌ها

- معماری Stateless: وضعیت کاربران (User State) به جای حافظه RAM، در دیتابیس ذخیره می‌شود. این امر باعث می‌شود ربات حتی پس از ریستارت شدن سرور، مرحله‌ای که کاربر در آن بوده را فراموش نکند.

- دیتابیس Async: استفاده از SQLAlchemy و aiosqlite برای جلوگیری از بلاک شدن Event Loop در ترافیک بالا.

- سیستم Caching: پیاده‌سازی مکانیزم کش برای قیمت بیت‌کوین جهت جلوگیری از ارسال درخواست‌های تکراری به API و مدیریت Rate Limit.

- داکرایز شده: آماده استقرار سریع با Docker.

- Functional Programming: ساختار کد به صورت تابعی و ماژولار طراحی شده است.

## 🛠 تکنولوژی‌های استفاده شده

- Language: Python 3.10+

- Framework: python-telegram-bot (v20+)

- Database: SQLite (with SQLAlchemy ORM Async)

- Containerization: Docker & Docker Compose

- Utilities: aiohttp, python-dotenv

## 🚀 راهنمای نصب و اجرا

### روش اول: اجرا با Docker (پیشنهادی)


1. کلون کردن مخزن:

```
git clone https://github.com/Mohsendrzh/BridgBot
cd BridgBot
```


2. تنظیم Environment:
فایل .env را بسازید و توکن را در آن قرار دهید:

> BOT_TOKEN=123456:ABC-DEF...


3. اجرا:
با استفاده از داکر کامپوز، سرویس را بالا بیاورید (فلگ -d برای اجرا در پس‌زمینه):

```docker-compose up -d --build```


نکته: دیتابیس به صورت خودکار در پوشه bot_data در مسیر فعلی ساخته و ذخیره می‌شود.

4. مشاهده لاگ‌ها:

```docker-compose logs -f```


5. توقف:

```docker-compose down```



### روش دوم: اجرا به صورت لوکال

یک محیط مجازی (Virtual Environment) بسازید و فعال کنید:

```
python -m venv venv
```
> در ویندوز:

```
venv\Scripts\activate
```
> در لینوکس/مک:

```
source venv/bin/activate
```

وابستگی‌ها را نصب کنید:

```
pip install -r requirements.txt
```


متغیرهای محیطی را تنظیم و ربات را اجرا کنید:

```
cp .env.example .env
```
 توکن را در فایل .env قرار دهید
python main.py


## 📂 ساختار پروژه

```
.
├── db.py              # لایه دیتابیس (مدل‌ها و کوئری‌های SQLAlchemy)
├── main.py            # نقطه ورود برنامه و هندلرها
├── utils.py           # ابزارها (API Client و Validation)
├── requirements.txt   # لیست کتابخانه‌ها
├── Dockerfile         # تنظیمات داکر
├── docker-compose.yml # ارکستراسیون داکر
└── README.md          # مستندات پروژه
```

## 💡 مکانیزم مدیریت وضعیت (State Management)

 برخلاف روش‌های معمول که از ConversationHandler (که وضعیت را در حافظه نگه می‌دارد) استفاده می‌کنند، این ربات از یک فیلد state در جدول users دیتابیس استفاده می‌کند.
 ### مزایا
- پایداری (Persistence): اگر سرور ربات ریستارت شود یا کرش کند، کاربر دقیقاً در همان مرحله‌ای که بود باقی می‌ماند و تجربه‌ی کاربری خراب نمی‌شود.
- کنترل بیشتری برای مدیریت کاربر میدهد
